# =============================================================================
# RAILWAY.COM ENVIRONMENT VARIABLES FOR API SERVICE
# =============================================================================
# 
# Copy these environment variables to your Railway API service dashboard
# Replace placeholder values with your actual values
#
# To set these in Railway:
# 1. Go to your Railway project dashboard
# 2. Select your API service
# 3. Go to Variables tab
# 4. Add each variable below
# =============================================================================

# Server Configuration (Railway automatically provides PORT)
NODE_ENV=production
ENVIRONMENT=production
HOST=0.0.0.0

# CORS Configuration - Link to your web app deployment
CORS_ORIGIN=https://your-web-app.railway.app
CORS_CREDENTIALS=true

# JWT Security Configuration
JWT_SECRET=your-super-secure-secret-key-at-least-32-characters-long
JWT_EXPIRES_IN=24h
JWT_ISSUER=nx-starter-api
JWT_AUDIENCE=nx-starter-pwa

# Database Configuration - Railway PostgreSQL
# When you add a PostgreSQL service in Railway, it automatically provides:
# DATABASE_URL - Use this by connecting your PostgreSQL service
DB_TYPE=postgresql
DB_ORM=typeorm

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=1000

# Application Settings
APP_NAME=Nx Starter API
APP_VERSION=1.0.0
API_PREFIX=/api

# Logging
LOG_LEVEL=info
LOG_FORMAT=json
LOG_ENABLE_COLORS=false

# =============================================================================
# RAILWAY SERVICE CONNECTIONS
# =============================================================================
# 
# To connect services in Railway:
# 1. Create PostgreSQL service: Add -> Database -> PostgreSQL
# 2. In API service variables, add reference: ${{Postgres.DATABASE_URL}}
# 3. Set CORS_ORIGIN to your web app URL: ${{WEB_SERVICE.RAILWAY_PUBLIC_DOMAIN}}
# 
# Example variable references:
# DATABASE_URL=${{Postgres.DATABASE_URL}}
# CORS_ORIGIN=https://${{web.RAILWAY_PUBLIC_DOMAIN}}
# =============================================================================