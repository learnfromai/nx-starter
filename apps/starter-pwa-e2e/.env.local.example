# E2E Test Environment Configuration for Local Storage (Dexie.js)
# Copy this file to .env.local when running E2E tests with local storage
# Usage: dotenv -e apps/starter-pwa-e2e/.env.local -- nx e2e starter-pwa-e2e

# Disable API backend for E2E tests (use local storage)
E2E_USE_API_BACKEND=false

# PWA application configuration
VITE_USE_API_BACKEND=false