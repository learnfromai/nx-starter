{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "pnpm install && pnpm run build:libs && pnpm run build:web"
  },
  "deploy": {
    "staticPublicPath": "/dist/apps/web"
  }
}